[package]
name = "mmap-js"
version = "0.1.0"
authors = ["Nehuén <github.com/nehu3n>"]
edition = "2024"

[lib]
crate-type = ["cdylib"]

[dependencies]
napi = "3.0.0"
napi-derive = "3.0.0"

[build-dependencies]
napi-build = "2"

[profile.release]
lto = true
codegen-units = 1
strip = "symbols"
